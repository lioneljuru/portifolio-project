import { Router } from 'express';
import EventController from '../Controller/EventController.js'

const router = Router();

// Not yet implemented
/*
router.get('/users', EventController.getUsers);
router.get('/users/:id', EventController.getUser);
router.post('/users', EventController.addUser);
router.patch('/users/:id', EventController.editUser);
router.put('/users/:id', EventController.updateUser);

export {router} */